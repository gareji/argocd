apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app1
  namespace: sample-app1
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: sample-app1
  template:
    metadata:
      labels:
        app: sample-app1
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
          command:
            - /bin/sh
            - -c
          args:
            - |
              mkdir -p /usr/share/nginx/html/app1

              cat <<'EOF' > /usr/share/nginx/html/app1/index.html
              <!DOCTYPE html>
              <html>
              <head>
                <title>Application 1</title>
              </head>
              <body style="
                background:#1565c0;
                color:white;
                font-family:Arial;
                text-align:center;
                padding-top:100px;
              ">
                <h1>APPLICATION 1</h1>
                <h2>SPA fallback works</h2>
                <p>Current URL:</p>

                <h3 id="path"></h3>

                <script>
                  document.getElementById("path").innerText =
                    window.location.pathname;
                </script>
              </body>
              </html>
              EOF

              cat <<'EOF' > /etc/nginx/conf.d/default.conf
              server {
                  listen 80;
                  server_name _;


                  root /usr/share/nginx/html;

                  location /app1/ {
                      try_files $uri $uri/ /app1/index.html;
                  }
              }
              EOF

              nginx -g 'daemon off;'